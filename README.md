# Student-Database-linked-list--C
Initial
This Program makes a database of students that each student have id,name,grade record
it uses Linked List and malloc and also saves each structure into a hex file and this file can be loaded if needed
it is tried to make this code efficient for memory and fast in execution
